{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "env": {
    "MONGO_URI": "@mongo_uri"
  },
  "commands": {
    "build": "tsc -b"
  },
  "routes": [
    {
      "src": "/.*",
      "dest": "/src/index.ts"
    }
  ]
}
